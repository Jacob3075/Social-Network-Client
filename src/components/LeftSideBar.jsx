import React from "react";
import makeStyles from "@material-ui/core/styles/makeStyles";

const useStyles = makeStyles(() => {});

const LeftSideBar = (props) => <div></div>;

export default LeftSideBar;
