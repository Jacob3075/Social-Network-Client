class Comment {
	constructor(id, userId, postId, comment, time, userName) {
		this.id = id;
		this.userId = userId;
		this.postId = postId;
		this.comment = comment;
		this.time = time;
		this.username = userName;
	}
}

export default Comment;
