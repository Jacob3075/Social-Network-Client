class Comment {
	constructor(id, userId, postId, comment, time) {
		this.id = id;
		this.userId = userId;
		this.postId = postId;
		this.comment = comment;
		this.time = time;
	}
}

export default Comment;
